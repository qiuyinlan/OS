JAVA PYTHON GO
内存泄漏：
内存一直被这个程序占着。
malloc的返回值就是一个指针。

rust会在编译的时候就避免这些情况，不过他编译非常慢。

gc就是程序员只关注业务，脏活系统干。
怎么判断是垃圾？
可达性。


‘
系统会遍历内存图，看活对象和垃圾。
当count=0的时候自动free掉。
引用计数法——智能指针。
- 致命缺点：循环引用。

标记-清除算法

堆区是连续的，malloc出来的内存也是。
- 会有内存碎片

标记-复制算法

弊端，cpu还要浪费一点，去扫描内存。
