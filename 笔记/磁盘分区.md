# why

可将os,用户数据，应用程序分开存储
系统崩溃，只需要格式化系统分区。

# what

分区决定了磁盘区域的范围（逻辑划分）
文件系统决定了==数据的组织方式==

两种方案，mbr  gpt

# mbr

==引导加载程序，分区表（分区大小，权限），魔数==。

一个扇区一般就是512byte，为了方便读取mbr,mbr也是512.

前三个直接，最后一个。
四个放8个。链表。

按理只能支持4个分区，实际上这样就可以拥有无限分区。


lsblk


扩展分区。


超级块，也有魔数。

## 文件系统

### 1.超级块

包含了文件系统的元数据和基本结构信息。比如文件系统的大小，空闲空间，文件系统类型。

### 2.文件分配表fat/索引节点inode

fat需要一个一个跳，所以被淘汰了。链表
位图，inode表。

inode表只有两个状态，用和没用，也就是位图来管理。
### 数据块


文件名是由目录来维护的。目录项包括文件和目录名+inode
文件名与inode映射，你输入文件名，映射到inode

## 日志

文件系统采取日志记录技术


fat文件系统



